apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: nagp
data:
  ENVIRONMENT_NAME: "local"
  DB_HOST: postgres.nagp.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: catalogdb
  DB_POOL_MAX_SIZE: "8"
  DB_POOL_MIN_IDLE: "2"
  FLYWAY_ENABLED: "true"
